server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/recipe_db?useSSL=false&serverTimezone=Asia/Taipei&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD} # 你本機的資料庫密碼

app:
  image:
    recipe-directory: ./images/recipes
